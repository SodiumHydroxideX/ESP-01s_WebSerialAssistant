# Web 串口助手 — 使用说明

## 概览

- 主要组件：终端、参数调试、自定义按钮、示波器。所有组件为可拖拽浮动窗口，支持大小调整、右键操作和布局持久化。
- 通过界面右侧或顶部“添加组件”打开需要的浮动窗口。
- 编辑/移动/调整后，布局会自动保存到 localStorage；也可手动导出为 JSON 文件备份或导入恢复。

## 详细功能说明

### 终端

- **功能**：显示接收到的数据，支持以文本或十六进制（hex）模式查看与发送数据。
- **常用操作**：
  - 发送：在发送输入框填写文本，点击“发送”或按 Enter。
  - 清空：点击“清除”可以快速清除显示内容。
  - 自动滚动：可在设置中开启/关闭；开启时输出新增时窗口自动滚到最底部。
  - 行数限制：可设置最大显示行数，超出部分自动裁剪以节省内存。

### 参数调试

- **功能**：创建多个可调参数（滑块、数值输入），并将这些参数映射到可发送命令模板中。
- **常用操作**：
  - 添加参数项：在参数窗口内添加新条目，配置最小/最大/步长/默认值。
  - 全局命令：在面板中定义包含占位符的命令，如 `SET {V1} {V2}`，保存后点击“发送”会把当前参数值按顺序替换占位符并发送。

### 自定义按钮

- **功能**：创建单击发送固定命令的按钮；支持延迟、重启启用条件等选项。
- **设置项说明（右键 → 设置）**：
  - 按钮显示文本：按钮页面上的文字。
  - 命令文本：单击按钮时发送的字符串（可包含换行或特殊转义，按实现解析）。
  - 按下后禁用按钮选项：
    - 无：只能通过刷新界面或在设置中手动关闭禁用使按钮恢复可用。
    - 延迟（ms）：发送后按钮短暂禁用，防止重复点击。
    - 串口收到指定指令：按下按钮后发送指令并进入禁用状态，直到串口收到指定数据恢复可用。

### 示波器

- **功能**：实时绘制多通道采样数据，带历史滑动、缩放与数据提示。
- **主要交互**：
  - 横向拖拽：按住鼠标在绘图区水平拖拽可平移时间轴（以时间为基准，能平滑处理不规则采样），竖直拖拽可平移纵轴。
  - 鼠标悬停：在靠近某采样点时显示该点的通道编号和数值（即使点标记被隐藏也会显示提示）。
  - 滚轮：改变时间轴缩放，方便查看完整曲线或局部特征。
  - Alt + 滚轮：改变纵轴缩放（放大/缩小幅度），方便观察细节。
  - 历史滑条：可控制显示窗口在整个采样历史中的位置。
  - 设置：可在示波器设置中切换是否显示样本点（点标记）。

### 布局与窗口操作

- 窗口拖拽：按住窗口标题栏拖动，释放后自动保存位置。
- 右键菜单：对窗口右键可执行“横向填充/纵向填充/全屏/锁定/调整大小/设置/删除”等操作。
- 自动置顶：新增或创建的窗口会自动调用提升函数把窗口置于最上层，避免创建后被其他窗口遮挡。
- 调整尺寸：可在右键菜单切换为“调整大小”模式进行精确拖动，完成后确认并保存。

## 布局导出与导入

- 导出：将当前布局（包含组件类型、位置、大小、设置）导出为 JSON 文件，便于备份或在另一台机器恢复。
- 导入：选择之前导出的 JSON 文件可以恢复相同的布局状态。
- 本地存储：页面会把最新布局自动保存到浏览器 localStorage 中，刷新页面会优先加载本地保存的布局。